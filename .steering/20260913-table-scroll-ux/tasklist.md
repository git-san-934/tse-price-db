# タスクリスト — 一覧テーブルのスクロールUX改善

- [x] 1. `index.html`: `.table-scroll`の直前に`.table-scroll-top`（内側にspacer div）を追加
- [x] 2. `index.html`: 「先頭へ戻る」ボタン要素を追加
- [x] 3. `assets/style.css`: `.table-scroll-top`のスタイル、`thead th`の`top`値調整、
      `.to-top-btn`のスタイルを追加
- [x] 4. `assets/app.js`: 上下スクロールバーの相互同期処理・幅の再計算(初期表示時・
      リサイズ時)を追加
- [x] 5. `assets/app.js`: 「先頭へ戻る」ボタンの表示制御・クリック処理を追加
- [x] 6. `index.html`のキャッシュバスティング用クエリ文字列を更新
- [x] 7. 品質チェック（ローカルサーバーでの要素・アセット到達確認、JS構文チェック）。
      ブラウザでの実際のスクロール挙動・見た目確認はChrome連携ツールが本セッションで
      利用できず未実施 — マージ前に`python -m http.server`での目視確認を推奨。

## 完了条件
- 上部でも横スクロール操作ができ、下部のスクロールバーと連動する。
- 「先頭へ戻る」ボタンで一覧の先頭までワンクリックで戻れる。
- 既存の検索・ソート・詳細パネル機能に影響がない。
